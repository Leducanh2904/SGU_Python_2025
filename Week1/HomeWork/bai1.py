# 1. You can calculate the surface area of a cube if you know the length of an edge. Write a
# program that takes the length of an edge (an integer) as input and prints the cube’s surface
# area as output.
a = input("Input the length of an edge ")
print("The cube's surface area : ", 6*a*a )